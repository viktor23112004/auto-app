export class Car {
    id: string
    name: string
    model: string
    price: number
    image: string

    constructor (id: string, name: string, model: string, price: number, image: string) {
        this.id = id
        this.name = name
        this.model = model
        this.price = price
        this.image = image

    }
}
